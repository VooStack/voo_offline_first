dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  # Testing dependencies
  bloc_test: ^9.1.2
  mocktail: ^0.3.0
  fake_async: ^1.3.1

  # Existing dependencies for code generation
  drift_dev: ^2.27.0
  json_serializable: ^6.9.5
  build_runner: ^2.5.4
  analyzer: ^7.5.2
  build: ^2.5.4
  source_gen: ^2.0.0
